{
  "name": "@sapcc/k8sclient",
  "version": "1.0.0",
  "description": "IN IE NOT SUPPORTED  * TextDecoder -> shim ? * Object.assign -> babel? * fetch  -> polyfil",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && babel src -d dist",
    "test": "jest dist"
  },
  "author": "Andreas Pfau",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sapcc/k8sClient.git"
  },
  "keywords": [
    "kubernetes",
    "js",
    "client"
  ],
  "bugs": {
    "url": "https://github.com/sapcc/k8sClient/issues"
  },
  "homepage": "https://github.com/sapcc/k8sClient#readme",
  "devDependencies": {
    "@babel/cli": "^7.12.13",
    "@babel/preset-env": "^7.12.13",
    "jest": "^26.6.3"
  },
  "dependencies": {
    "regenerator-runtime": "^0.13.7",
    "text-encoding": "^0.7.0"
  }
}
